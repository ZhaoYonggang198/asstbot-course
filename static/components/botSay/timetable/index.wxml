<view wx:if="content.body||content.pages">
    <text>课程提醒:</text>
    <block wx:if="{{content.type =='page-list'}}">
        <bot-say-timecell   wx:for="{{content.pages}}" content="{{item}}"/>
    </block>
    <block wx:else>  
        <bot-say-timecell/>
    </block>   
</view>