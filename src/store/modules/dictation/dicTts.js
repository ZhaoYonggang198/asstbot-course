const state = {
  audioUrl: ''
}

const mutations = {
  saveId (state, id) {
    state.id = id
  }
}

const actions = {
  getAudioUrl((dispatch, commit), param) {
}
}

export default {
  state,
  mutations,
  actions
}
